@page
@using Eva.Workflow
@model Eva.Pages.Metroplan.Analista.IndexModel
@{
    ViewData["Title"] = "Fila de Análise - Fretamento Eventual";
    bool isAdmin = User.IsInRole("ADMIN");
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4 pb-2 border-bottom">
        <div>
            <h1 class="h2"><i class="bi bi-inboxes-fill text-primary me-2"></i>Fila de Análise</h1>
            <p class="text-muted">Submissões aguardando verificação de documentos e dados.</p>
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">Data da Solicitação</th>
                            <th>Tipo</th>
                            <th>Identificador</th>
                            <th>Status</th>
                            <th>Atribuído A</th>
                            <th class="text-end pe-4">Ação</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!Model.Fila.Any())
                        {
                            <tr>
                                <td colspan="6" class="text-center py-5 text-muted">
                                    <i class="bi bi-check2-circle display-4 d-block mb-2 text-success"></i>
                                    A fila está vazia. Excelente trabalho.
                                </td>
                            </tr>
                        }
                        else
                        {
                            foreach (var item in Model.Fila)
                            {
                                bool isMyLock = item.Status == SubmissaoWorkflow.EmAnalise && item.AnalistaDaSubmissao == Model.AnalistaAtual;
                                bool isLockedByOther = item.Status == SubmissaoWorkflow.EmAnalise && item.AnalistaDaSubmissao != Model.AnalistaAtual;

                                <tr class="@(isMyLock ? "table-info" : "")">
                                    <td class="ps-4">@((item.SubmetidoEm ?? item.CriadoEm).ToString("dd/MM/yyyy HH:mm"))</td>
                                    <td>
                                        <span class="badge bg-secondary text-uppercase">@item.EntidadeTipo</span>
                                    </td>
                                    <td class="fw-bold">@item.EntidadeId</td>
                                    <td>
                                        <span class="badge @SubmissaoWorkflow.GetBadgeClass(item.Status)">
                                            <i class="bi @SubmissaoWorkflow.GetBadgeIcon(item.Status)"></i>
                                            @SubmissaoWorkflow.GetDisplayLabel(item.Status)
                                        </span>
                                    </td>
                                    <td>
                                        @if (isLockedByOther)
                                        {
                                            <span class="text-danger small"><i class="bi bi-person-fill-lock"></i> @item.AnalistaDaSubmissao</span>
                                        }
                                        else if (isMyLock)
                                        {
                                            <span class="text-primary small fw-bold">Você</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted small">-</span>
                                        }
                                    </td>
                                    <td class="text-end pe-4">
                                        @if (isLockedByOther && isAdmin)
                                        {
                                            <form method="post" asp-page-handler="Desatribuir" class="d-inline">
                                                <input type="hidden" name="submissaoId" value="@item.SubmissaoId" />
                                                <button type="submit" class="btn btn-sm btn-danger me-2" onclick="return confirm('Tem certeza que deseja desatribuir esta submissão?');">
                                                    <i class="bi bi-unlock-fill"></i> Desatribuir
                                                </button>
                                            </form>
                                        }
                                        <a href="/Metroplan/Analista/Revisao?submissaoId=@item.SubmissaoId" class="btn btn-sm btn-primary">
                                            <i class="bi bi-eye"></i> Visualizar
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
